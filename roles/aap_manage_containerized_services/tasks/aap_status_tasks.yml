---

- name: Get {{ item }} status
  ansible.builtin.systemd_service:
    name: "{{ item }}"
    scope: user
  register: svc_state

- name: Print {{ item }} status
  ansible.builtin.debug:
    msg: "{{ item }} state is {{ svc_state.status.SubState }}"