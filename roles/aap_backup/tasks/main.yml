---
# tasks file for aap_backup

#
# -----------------------------------------------------------------------------
# Prepare for AAP work
# -----------------------------------------------------------------------------
#

- name: Include backup tasks
  ansible.builtin.include_tasks:
    file: "{{ (aap_backup_containerized | bool) | ternary('containerized.yml', 'rpm.yml') }}"
  tags:
    - backup
...
